{
  "name": "pokedex-master",
  "version": "0.1.0",
  "author": "1trickPwnyta",
  "scripts": {
	"prestart": "node -e \"fs.writeFileSync('./static/meta.json', JSON.stringify({ version: require('./package.json').version }))\"",
	"start": "http-server ./static -a 0.0.0.0 -p 80 -d false -s",
	"pretest": "npm run prestart",
	"test": "http-server ./static -c-1"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
